{
  "name": "qumo-web",
  "version": "1.0.0",
  "private": true,
  "main": "electron/main.js",

  "scripts": {
    "start": "electron .",
    "intro-workbench": "node tools/intro-quiz-workbench/server.js",

    "tunnel": "node server/run-tunnel.js",

    "dev": "concurrently --kill-others --kill-signal SIGINT -n app,tunnel -c auto \"npm:start\" \"npm:tunnel\"",

    "pack": "electron-builder --dir",
    "dist": "electron-builder",
    "dist:win": "electron-builder --win",
    "dist:mac": "electron-builder --mac"
  },

  "build": {
    "appId": "com.example.qumo.web",
    "productName": "QUMO-WEB",
    "asar": true,

    "files": [
      "electron/**",
      "server/**",
      "public/**",
      "!tools/**",
      "!QUMO_web_LOGO.af",
      "package.json"
    ],

    "extraResources": [
      {
        "from": "assets",
        "to": "assets"
      },
      {
        "from": "mods",
        "to": "mods"
      },
      {
        "from": "config",
        "to": "config"
      },
      {
        "from": "bin/cloudflared.exe",
        "to": "bin/cloudflared.exe"
      }
    ],

    "win": {
      "icon": "build/icon.ico",
      "target": [
        "nsis",
        "portable"
      ]
    },

    "nsis": {
      "installerIcon": "build/icon.ico",
      "uninstallerIcon": "build/icon.ico"
    },

    "directories": {
      "output": "dist_electron"
    }
  },

  "dependencies": {
    "express": "^4.19.2",
    "qrcode": "^1.5.4",
    "ws": "^8.18.0"
  },

  "devDependencies": {
    "concurrently": "^9.2.1",
    "electron": "^40.0.0",
    "electron-builder": "^26.4.0"
  }
}
